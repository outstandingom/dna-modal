services:
  - type: web
    name: dna-modal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn knowledge_graph_env:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
